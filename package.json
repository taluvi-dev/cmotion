{
  "name": "cmotion",
  "private": true,
  "description": "cmotion video and animation DSL — monorepo",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @cmotion/web dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
