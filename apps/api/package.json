{
  "name": "@cmotion/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "cmotion hosted render API — Cloudflare Worker + Containers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251110.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
