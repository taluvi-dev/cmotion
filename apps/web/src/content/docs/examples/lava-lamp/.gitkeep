# Lava lamp example — placeholder folder.
# Brief/reference image: /img/lava-lamp.png (apps/web/public/img/lava-lamp.png).
# The example source + index.mdx land once the design is set. (.gitkeep
# keeps the dir under version control and is ignored by Starlight's
# content loader.)
